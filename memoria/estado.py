from datetime import datetime


estado_sarah = {
    "modo": "normal",
    "nivel_iniciativa": "medio",
    "puede_interrumpir": True,
    "ultima_interaccion": datetime.now().isoformat(timespec="seconds"),
    "ultimo_mensaje_autonomo": None,
    "objetivo_actual": "Construir a Sarah como IA personal con voz, memoria, soporte y autonomía controlada."
}